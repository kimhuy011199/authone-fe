interface UserInformationFormProps {
  onClose: () => void;
}

const UserInformationForm = (props: UserInformationFormProps) => {
  const { onClose } = props;
  return <div>UserInformationForm</div>;
};

export default UserInformationForm;
